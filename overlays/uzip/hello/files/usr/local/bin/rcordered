#!/bin/sh

# TODO: Find a way to replace rcorder(8) with something
# that works without arguments
# https://github.com/helloSystem/ISO/issues/308

if [ "$#" == 0 ] ; then
  /sbin/rcorder /etc/rc.d/* /usr/local/etc/rc.d/*
else
  /sbin/rcorder "$@"
fi

